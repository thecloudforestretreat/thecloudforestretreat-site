THE CLOUD FOREST RETREAT FINAL PRODUCTION SEO UPSCALE + QA + TSV PATCH-SAFE WORKFLOW

Use this prompt for TheCloudForestRetreat.com page-by-page SEO upscale work after the master CSV has been generated.

INPUTS TO PROVIDE EACH TIME
- English HTML page file
- Spanish HTML page file, if paired
- English TSV row copied from the TCFR master SEO CSV
- Spanish TSV row copied from the TCFR master SEO CSV, if paired
- Any relevant global files only if changed: header.html, footer.html, site.css, header.css, site.js

PRIMARY GOAL
Perform a final production pass for the uploaded page pair and return:
1. Complete updated English downloadable .txt file
2. Complete updated Spanish downloadable .txt file, if paired
3. Full updated English TSV row
4. Full updated Spanish TSV row, if paired
5. Short production QA summary

SITE CONTEXT
The Cloud Forest Retreat is an owned lodging/nature retreat site in the Choco Andino / Quito cloud forest region. The SEO strategy should strengthen direct booking intent, cloud forest authority, room/stay clarity, birding/nature relevance, sustainability trust, and practical travel planning value.

STRATEGIC PRIORITIES
1. Production stability
2. Correct EN/ES pairing
3. Direct booking and inquiry conversion
4. Cloud forest / Quito / Choco Andino authority
5. Room, lodge, and experience clarity
6. Technical SEO QA
7. TSV synchronization
8. Global CSS standardization
9. Shared component consistency
10. Elimination of unnecessary page-level CSS

GLOBAL CSS / COMPONENT CONTRACT RULE

All updated pages must honor the TCFR global component system.

Do not create page-specific typography, button, card, badge, kicker, or section styles unless absolutely required for layout exceptions.

Normalize all pages into shared reusable component classes.

Required shared global structure:
- tcfr-page
- tcfr-shell
- tcfr-section
- tcfr-section-head
- tcfr-section-title
- tcfr-section-intro
- tcfr-grid
- tcfr-card
- tcfr-card-title
- tcfr-card-text
- tcfr-actions
- tcfr-pill
- btn
- btn primary
- btn secondary

Required shared hero system:
- tcfr-hero
- tcfr-hero-media
- tcfr-hero-image
- tcfr-hero-copy
- tcfr-kicker
- tcfr-display
- tcfr-lead

GLOBAL TYPOGRAPHY STANDARD

All pages must inherit typography from the global system.

Do not create page-level heading font weights or font sizes.

The homepage is the visual authority reference for all future pages.

Standardized typography rules:
- Hero display weight: 800
- Hero display alignment: left
- Hero display line-height: shared globally
- Section title weight: 800
- Card title weight: 800
- Kicker alignment: left
- Kicker color: dark green
- Kicker transform: uppercase
- Kicker spacing: shared globally
- Kicker style: plain text, not bubble/pill, unless intentionally using tcfr-pill
- No centered hero kickers unless the layout is intentionally centered
- No white kicker text on light backgrounds
- No page-specific hero heading scaling
- No page-specific heading tracking
- No page-specific serif weight overrides

CRITICAL GLOBAL CSS RULE

Do not continuously patch site.css with isolated page-specific overrides.

Instead:
1. Analyze the existing global system first
2. Normalize inconsistent page HTML into shared components
3. Remove unnecessary page-level styling
4. Consolidate repeated patterns into reusable global classes
5. Ensure all pages inherit the same visual language

The objective is:
ONE shared visual system
NOT multiple page-specific systems.

PAGE STANDARDIZATION RULE

Before generating CSS:
- Compare the uploaded page against existing TCFR production patterns
- Reuse existing hero structure whenever possible
- Reuse existing section structures whenever possible
- Reuse existing card structures whenever possible

If a visual inconsistency exists between pages:
- Prefer updating HTML structure to honor the global system
- Avoid introducing one-off CSS fixes

CSS CONSOLIDATION RULE

When updating site.css:
- Consolidate duplicated typography rules
- Consolidate duplicated spacing systems
- Consolidate duplicated hero rules
- Consolidate duplicated card rules
- Consolidate duplicated button rules
- Consolidate duplicated grid rules

Avoid:
- isolated per-page heading rules
- page-specific hero font rules
- page-specific kicker styles
- deeply nested override selectors
- emergency !important patches unless absolutely unavoidable

MOBILE CONSISTENCY RULE

Mobile layouts must inherit the same typography scale relationships as desktop.

Do not:
- arbitrarily reduce heading weights
- introduce different mobile serif weights
- center-align hero copy on mobile unless intentional
- create mobile-only visual systems


CRITICAL TSV RULE
Never regenerate TSV rows from memory.
Always patch the original TSV row as the base structure.
Preserve exact column count, exact column order, exact empty-column placement, and exact trailing empty columns.

TSV COLUMN SAFETY
The TCFR master CSV contains 138 columns.
The duplicate header names priority and status must remain exactly where they are.
Do not collapse duplicate headers.
Do not remove blank placeholder fields.
Do not insert new values into url_structure_issue_flags, seo_issue_flags, or content_quality_flags unless that exact field is intentionally being updated.

CRAWL FIELD PRESERVATION
Crawl-observed fields are historical crawl data from Screaming Frog. Preserve them unless a new crawl export is provided.
Never manually rewrite:
- crawl_title
- crawl_title_length
- crawl_meta_description
- crawl_meta_description_length
- crawl_h1
- crawl_h1_length
- crawl_h2_1
- crawl_canonical
- crawl_last_modified
- crawl_language
- crawl_source_file
- crawl response/status metrics
- crawl word count
- crawl inlink/outlink metrics

CQ / CR / CS SAFETY
Before returning TSV rows, verify:
- crawl_last_modified remains in its original column
- crawl_language remains in its original column
- crawl_source_file remains in its original column
- no downstream fields shifted

GSC RULE
GSC fields represent the imported report state. Do not manually recalculate them during page-upscale work unless the user provides a new GSC export.
When generating a new master CSV, aggregate GSC URL variants by normalized canonical URL. For example, http, https, www, and non-www variants of the same path must be combined before filling clicks, impressions, CTR, and weighted average position.

SEO UPSCALE REQUIREMENTS
Review and improve where needed:
- title
- meta description
- H1/H2 structure
- semantic coverage
- internal links
- booking CTAs
- room/stay clarity
- FAQ usefulness
- schema
- breadcrumbs
- Open Graph
- Twitter metadata
- canonical
- hreflang
- image alt text
- analytics attributes

DO NOT
- redesign stable pages unnecessarily
- rewrite global CSS architecture unless broken
- duplicate header/footer loaders
- add generic filler content
- keyword stuff
- force unrelated internal links
- overwrite crawl fields manually
- place the word resolved into issue-flag columns unless that is explicitly the correct field value

SCHEMA RULES
JSON-LD must parse as valid JSON.
All schema objects must remain inside the same @graph array.
Do not append orphan schema objects after the @graph or root object.
No trailing commas.
Use appropriate types such as WebPage, Article, LodgingBusiness, FAQPage, BreadcrumbList, Organization, and WebSite.

SPANISH RULE
Spanish visible copy must use proper LATAM Spanish accents.
Never add accents to URLs, slugs, file paths, IDs, classes, schema URLs, analytics event names, or JavaScript identifiers.

ANALYTICS RULE
Preserve centralized tracking architecture and data-analytics attributes.
Verify CTA, language switch, FAQ, internal link, and outbound link tracking where present.
Do not rebuild analytics unnecessarily.

READY TO GO LIVE STANDARD
Only mark ready/go-live style fields as complete when:
- SEO metadata is complete
- canonical/hreflang is correct
- schema parses
- metadata parity is complete
- header/footer/site.js loading is stable
- CTAs work
- internal links are valid
- EN/ES pairing is correct
- Spanish accents are correct
- TSV row alignment is verified
- Global CSS consistency is verified
- Typography consistency is verified
- Shared component compliance is verified

FINAL OUTPUT FORMAT
Return:
1. Download link for complete EN .txt file
2. Download link for complete ES .txt file, if applicable
3. Plain TSV code block with full updated TSV rows
4. Very short QA note

Do not use markdown tables for TSV.
Do not quote TSV fields.
Do not remove trailing tabs.
Do not return partial snippets.

DOMAIN-SAFE GSC NORMALIZATION RULE
When importing Google Search Console page data, never normalize URLs across different hosts or subdomains.
Allowed hosts for The Cloud Forest Retreat master CSV:
- thecloudforestretreat.com
- www.thecloudforestretreat.com

Do NOT merge, rewrite, or convert these into main-site rows:
- blog.thecloudforestretreat.com
- staging.thecloudforestretreat.com
- any other subdomain or external host

If GSC contains blog or staging URLs, exclude them from the main-site master CSV unless the user explicitly asks for a separate blog/staging audit. Never convert blog.thecloudforestretreat.com/YYYY/MM/post.html into https://thecloudforestretreat.com/YYYY/MM/post.html/.

CRAWL BLANK FIELD RULE
Blank crawl_* fields are valid when a URL exists in the sitemap or GSC export but does not appear in the Screaming Frog internal HTML crawl export. Do not fill crawl_status_code, crawl_status, crawl_indexability, crawl_depth, crawl_title, crawl_meta_description, crawl_h1, crawl_canonical, crawl_language, or crawl_source_file by inference. Instead, leave the crawl fields blank and use audit_status/source flags to show why the row exists.

COUNTERPART EXPECTED URL RULE
Only populate counterpart_expected_url when the page is a real paired multilingual URL. Leave it blank for standalone, GSC-only, crawl-only, technical, or unpaired pages unless a counterpart can be verified from sitemap, hreflang, or explicit site structure.

MASTER CSV EXCLUSION RULE
The page-level master SEO CSV should not include non-page technical resources such as /assets/ files, /cdn-cgi/ email-protection URLs, scripts, CSS files, images, or other implementation assets unless the user explicitly requests a technical asset crawl audit.
